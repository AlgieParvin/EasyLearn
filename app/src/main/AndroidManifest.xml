<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ua.nure.crew.easylearn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.topics.TopicsActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
        </activity>
        <activity
            android:name=".view.testTasks.TestTasksActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".view.testTasks.WatchMistakesActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".view.vocabulary.VocabularyActivity" />
        <activity android:name=".view.type.TaskTypeActivity" />
        <activity
            android:name=".view.enterTest.DialogueActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog" />
        <activity android:name=".view.rebuses.RebusesActivity" />

    </application>

</manifest>